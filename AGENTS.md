# Repository Guidelines

## 项目结构与模块划分
本项目是基于 Maven 的 Spring Boot 4 + Java 21 工程，主代码位于 `src/main/java/com/hp/javabase`，按职责分层：
- `controller/`：接口层，只负责收参、校验基础参数、返回统一响应
- `service/`：业务编排与事务边界
- `mapper/`：MyBatis-Plus Mapper 接口
- `model/entity`、`model/dto`、`model/vo`：实体、入参、出参模型
- `common/`：通用响应、异常、注解、切面等基础组件

资源文件位于 `src/main/resources`：
- `application.yml`：运行配置
- `mapper/*.xml`：MyBatis XML
- `sql/*.sql`：建表或初始化脚本

测试代码位于 `src/test/java/com/hp/javabase`。

## 构建、测试与本地开发命令
- `./mvnw clean compile`：编译并检查 Spring/Java 代码是否通过
- `./mvnw test`：执行测试
- `./mvnw spring-boot:run`：本地启动应用
- `docker compose up -d`：启动 MySQL、Redis
- `docker compose logs -f mysql`：查看 MySQL 日志

## 编码规范与命名约定
统一使用 4 空格缩进，类名使用 `PascalCase`，方法和字段使用 `camelCase`，常量使用 `UPPER_SNAKE_CASE`。优先使用构造器注入，不使用字段注入。实体类与表结构保持一致，Mapper 与 XML 命名保持成对，例如 `UserMapper` 与 `UserMapper.xml`。

## Java 工程质量约束
Controller 不直接写 SQL，不承载复杂业务规则。Service 不做纯透传，必须承载明确业务语义；若仅一行转调，应考虑删除冗余层。禁止把数据库实体直接作为复杂接口入参，新增接口优先定义 DTO/VO。统一使用项目现有 `BaseResponse`、全局异常处理和错误码，不在接口层随意返回裸字符串。SQL 以可读性优先，复杂查询写入 XML，并关注索引、分页和 `EXPLAIN` 结果，避免 N+1 查询与无条件全表扫描。

## 测试规范
使用 JUnit 与 Spring Boot Test。测试类命名采用 `*Tests` 后缀，参考 `JavaBaseApplicationTests`。

新增或修改功能时，测试要求如下：
- 新增业务逻辑、接口分支、异常场景时必须补测试，不能只补主流程。
- 新增接口时，必须检查是否存在对应接口测试文件或测试用例。
- 每个新增接口至少覆盖成功路径、参数校验失败、业务异常、边界输入等测试场景。
- 边界测试需根据接口特性覆盖空值、null、非法枚举值、长度边界、分页边界、重复提交或幂等等必要情况。
- 涉及数据库写入、状态流转、异步处理或外部依赖的接口，应补 service 测试或集成测试，避免只测 controller 表层返回。
- 提交前必须执行本次新增功能相关测试；如果改动影响公共模块、基础组件或横切逻辑，还应补充执行更大范围测试。
- 若无法执行相关测试，需明确说明原因与风险，不能默认跳过。

提交前最少执行以下校验：
- `./mvnw clean compile`
- `./mvnw test`
- 本次新增功能相关的目标测试类或目标测试方法

代码评审时，默认将“新增接口缺失测试”视为明确问题，而非优化建议。

## 本地 Skill 加载
涉及 Java 代码实现、重构、补测试、补校验时，优先加载并遵循本工程内的本地 skill：

- `.codex/skills/java-code-style.md`

涉及 Java 代码评审、Review、变更风险分析、测试覆盖检查时，优先加载并遵循本工程内的本地 skill：

- `.codex/skills/java-code-review.md`

两者职责边界如下：
- `java-code-style`：用于写代码、改代码、重构代码、补测试，关注实现过程中的分层、命名、异常、日志、安全、SQL 和测试规范。
- `java-code-review`：用于审代码、查风险、查回归、查测试缺口，关注改动后的正确性、影响范围、行为回归和提交前门禁。
- `AGENTS.md` 只保留仓库级硬性约束；通用的实现细节和评审细节以各自 skill 为准，避免重复维护。

## 代码改动与 Commit 要求
当用户与 Codex 交互过程中涉及代码新增、删除、修改、重构时，必须执行以下要求：

- 先按照本工程 `AGENTS.md` 中的仓库规范，以及 `.codex/skills/java-code-style.md`、`.codex/skills/java-code-review.md` 中的约束，对改动代码进行自检。
- 自检范围不仅包含语法和编译，还包括分层职责、命名规范、异常处理、日志、安全、SQL、测试覆盖、边界场景和提交前门禁。
- 如果新增了接口、修改了接口行为、调整了数据库访问逻辑、修改了公共组件或横切逻辑，必须显式检查这些改动是否符合当前 skill 与本目录代码规范。

在执行 `commit` 之前，必须明确说明以下内容：
- 本次改动涉及哪些文件。
- 本次改动属于什么类型，且必须从以下固定值中选择一个：`feature`、`fix`、`refactor`、`test`、`config`、`docs`。
- 本次改动影响了哪些业务功能、接口、数据流或模块边界；如果涉及接口，必须尽量明确写出接口路径，例如 `/demo/login`、`/demo/logout`。
- 是否涉及数据库变更，包括表结构、SQL、索引、初始化数据、字段映射等。
- 是否涉及接口影响，包括新增接口、修改接口、删除接口、返回字段变化、兼容性变化等；如果有接口影响，必须尽量明确写出对应接口路径。
- 是否存在潜在影响范围，例如公共组件、公共返回结构、数据库字段映射、事务行为、缓存逻辑、鉴权逻辑等。

在执行 `commit` 之前，必须完成以下验证：
- 先对本次改动做一遍自测，确认主流程、关键分支、边界输入、异常路径至少经过一次主动验证，而不是只依赖静态阅读。
- 检查注释规范是否满足要求，包括类头部说明、关键核心方法顶部注释、以及注释内容是否与当前实现保持一致。
- 执行与本次改动直接相关的测试类或测试方法。
- 如果改动影响公共模块、基础组件、共享逻辑或核心链路，还必须补充执行更大范围测试。
- 确认测试全部通过后，才允许执行 `commit`。
- 如果相关测试未执行、执行失败、或覆盖范围不足，不允许在默认情况下直接 `commit`，必须先补齐说明或补齐测试。

如果代码不满足当前 skill 或本目录规范要求，应补充输出 `.codex/` 目录下的检查报告或重构建议，作为后续处理依据：
- 建议目录：`.codex/reports/`
- 统一文件：`.codex/reports/code-quality-review.md`
- 报告内容至少包含：问题文件、问题方法、违反规则、影响范围、修复建议、是否阻塞 commit。
- 当存在未满足规范但暂未修复的问题时，不应直接静默提交；应先给出报告，再由用户决定是否接受重构建议。

仓库内已提供本地 Git Hook 实现，位于 `.githooks/`：
- `pre-commit`：输出暂存改动文件、提示可能影响的业务层次，并在提交前执行编译与测试门禁；当 controller 改动缺少对应测试文件时直接阻止提交。
- `commit-msg`：强制提交信息中明确填写改动文件、影响范围、测试执行情况。
- `install.sh`：用于执行 `git config core.hooksPath .githooks` 并安装本地 Hook。

说明：
- Hook 提供的是基础自动门禁，不等于完整评审。
- 即使 Hook 通过，仍需按当前 skill 检查边界场景、异常路径、业务影响范围和测试充分性。

当用户输入或明确表达以下提交意图时，例如：
- `提交`
- `推送`
- `帮我提交代码`
- `帮我提交并推送代码`
- 其它等价的 commit / push 请求

默认按如下方式执行：
- 由 Codex 负责整理并生成详细的 commit message，而不是要求用户手写完整提交说明。
- Codex 在生成 commit message 时，必须基于当前改动自动总结：改动文件、影响范围、测试执行情况。
- 在执行 commit 之前，Codex 必须先完成一遍与本次改动对应的自测，并在提交说明或最终说明中明确写出自测与测试执行情况。
- Codex 应通过标准的 `git commit` 流程执行提交，以确保 `.githooks/pre-commit` 和 `.githooks/commit-msg` 自动触发。
- 如果用户请求推送，则在 commit 成功且本地 Hook 校验通过后，再执行标准 `git push`，从而保持 Hook 与提交门禁生效。
- 如果测试未通过、Hook 未通过、或影响范围不清晰，Codex 不应直接提交或推送，而应先补齐说明、补齐测试或修复问题。

推荐的提交信息格式如下：

```text
新增登录接口测试覆盖

Scope: feature
Files: src/main/java/com/hp/javabase/controller/AuthController.java, src/test/java/com/hp/javabase/controller/AuthControllerTests.java
Impact: /demo/login、/demo/logout、鉴权流程、登录返回结构
DB: 无数据库结构变更，未修改索引，涉及用户查询 SQL
API: 修改 /demo/login 返回结构；新增 /demo/logout
Tests: 自测登录主流程与异常分支通过；./mvnw test 通过
```

## 表与字段规范
评审数据库设计、实体映射、SQL 变更时，默认按较高标准检查：

- 表名、字段名应使用清晰、稳定、可读的业务命名，优先采用小写加下划线风格。
- 禁止使用语义不明的缩写、拼音缩写、`status1`、`type2` 之类不可维护命名。
- 主键、唯一索引、普通索引设计应与查询路径匹配；新增查询条件时需关注是否需要补索引。
- 字段类型必须合理：金额禁止使用浮点；文本长度不应随意放大；状态字段需有清晰取值定义。
- 通用审计字段如创建时间、更新时间、创建人、更新人、逻辑删除标记应按项目需要保持一致。
- 避免过度允许 `null`、默认值语义不清、保留字冲突、时间字段语义混乱等问题。
- Entity、DTO、VO、Mapper XML 与数据库表结构应保持一致，避免字段缺失、类型不匹配、映射错误。

## 代码规范补充
除基础命名和格式规范外，评审时重点关注以下质量要求：

- 方法职责应单一，默认单个方法不超过 80 行；出现超长方法、深层嵌套、过多布尔分支和难以维护的条件拼接时，应优先拆分。
- 禁止魔法值、散落硬编码、重复逻辑，公共规则应提炼到合适位置。
- 异常处理必须统一，禁止吞异常、只打印日志不抛出、直接向接口暴露底层异常。
- 日志应包含必要业务上下文，禁止输出敏感信息，避免无意义日志刷屏。
- 注释应解释设计原因、边界或约束，不应写成代码翻译；过期注释应及时清理。
- 默认参考《Java 开发手册》的强制类规则进行实现与评审，重点检查命名规范、`@Override`、`BigDecimal` 使用、包装类型比较、`finally` 返回、复杂条件拆分等高频问题。

新增或显著修改 `.java` 文件时，补充注释要求如下：
- 类、工具类、枚举、配置类、切面、异常类在文件头部应增加清晰注释，说明该类型的作用、提供的核心功能、依赖的关键组件或外部能力。
- 关键核心方法在方法顶部应增加详细注释，便于他人快速理解该方法解决什么问题、主要流程、关键依赖和边界约束。
- 对极简单的 getter、setter、短小纯工具方法，可不强制增加大段注释；但复杂业务方法、事务入口、状态流转、核心校验、缓存/鉴权/限流相关方法默认应补注释。

## 异常、日志与安全规范
评审异常处理、日志输出和安全相关代码时，默认检查以下事项：

- 异常不能替代正常流程控制，`catch` 后必须有清晰处理策略，禁止吞异常。
- 涉及事务的方法在捕获异常后必须关注回滚语义，避免逻辑成功返回但数据未正确提交或回滚。
- 资源释放优先使用 try-with-resources；不得在 `finally` 中写 `return`。
- 日志统一走 `SLF4J`，日志内容要能还原现场，但不得输出密码、Token、密钥、身份证号、手机号明文等敏感信息。
- 用户输入、排序字段、分页大小、重定向地址、动态 SQL 参数都必须校验，避免 SQL 注入、XSS、越权、ReDoS 和资源滥用。
- 用户私有数据、管理类接口、资源消耗型接口必须检查鉴权、防刷、频控和脱敏。

## 工程规范补充
评审工程结构和模块协作时，默认遵循以下原则：

- controller 只负责协议层职责，不直接写 SQL，不承载复杂业务规则。
- service 必须体现业务语义与事务边界；如果只是简单转调，应评估是否存在冗余层。
- mapper 只负责数据访问，不混入业务分支判断。
- 复杂接口优先定义 DTO/VO，避免数据库实体直接暴露到外部协议。
- 公共能力应沉淀复用，避免复制粘贴式工具类、转换类、常量类蔓延。
- 敏感配置、密钥、密码不得硬编码；环境差异应通过配置管理解决。
- 新增依赖必须有明确收益，避免无必要地引入重量级库或重复能力依赖。
- 变更不得轻易破坏模块边界、制造循环依赖或让基础层反向依赖业务层。
- 参考《Java 开发手册》的工程结构与设计规约，新增复杂状态流转、长调用链、核心数据结构变更时，应补充必要的设计说明并评估异常流程与边界场景。

## 提交与合并请求规范
当前仓库尚无提交历史，建议使用简短、明确的祈使句提交信息，例如 `新增 AI 对话接口`、`补充 user 表 mapper xml`。每次提交只聚焦一个变更点。PR 需说明变更目的、影响范围、配置或表结构调整，以及接口变更示例。

## 安全与配置要求
禁止提交真实密码、Token、API Key。数据库账号密码、`OPENAI_API_KEY` 等敏感配置应使用环境变量或本地覆盖文件。排查慢 SQL 时优先开启 MySQL 慢查询日志，并结合 `EXPLAIN` 与应用日志分析。
